@using System.Web.Optimization;
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    @Styles.Render("~/Content/css")
</head>
<body>
    <div class="alert alert-primary">Index page</div>
    <div class="row">
        <div class="col-sm-1">
            <ul class="nav flex-column">
                <li class="nav-item">@Html.ActionLink("Home", "Index", "Home")</li>
                <li class="nav-item">@Html.ActionLink("Vehicles", "Vehicles", "Home")</li>
                <li class="nav-item">@Html.ActionLink("Autoparts", "Autoparts", "Home")</li>
                <li class="nav-item">@Html.ActionLink("Services", "Services", "Home")</li>
            </ul>
        </div>
        <div class="col-sm-1">
            @{Html.RenderAction("Services", "Home");}
        </div>
    </div>
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @*@RenderSection("scripts", required: false)*@
</body>
</html>

